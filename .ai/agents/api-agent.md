# API Agent

Goal:
- Generate or refine HTTP routes/controllers based strictly on API section in spec.

Rules:
- Keep contract names and fields aligned to spec.
- Include input validation and standard error responses.
- Do not add endpoints not listed in spec.
